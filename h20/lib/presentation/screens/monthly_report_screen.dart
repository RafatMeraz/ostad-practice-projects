import 'package:flutter/material.dart';

class MonthlyReportScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Monthly Report'),
      ),
      body: Center(
        child: Text('This is the Monthly Report Screen'),
      ),
    );
  }
}